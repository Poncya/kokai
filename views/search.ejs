<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8" />
  <title>検索結果</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <header class="header">
    <div class="container header__inner">
      <h1 class="brand">検索結果</h1>
      <a class="btn" href="/">メインへ戻る</a>
    </div>
  </header>

  <main class="container stack-lg">
    <div class="card card--flat">
      <p>「<strong><%= keyword %></strong>」の検索結果：<%= results.length %>件</p>
    </div>

    <% if (!results || results.length === 0) { %>
      <div class="card"><p class="muted">該当する記事は見つかりませんでした。</p></div>
      <p><a class="btn" href="/">メインへ戻る</a></p>
    <% } else { %>
      <% results.forEach(p => { %>
        <article class="card">
          <h3 class="card__title"><%= p.title %></h3>
          <p class="meta">投稿日: <%= p.date %></p>
          <p class="card__body"><%= p.body %></p>
        </article>
      <% }) %>
    <% } %>
  </main>
</body>
</html>
